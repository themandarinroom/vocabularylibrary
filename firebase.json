{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "tools/word-randomizer/firestore-live-sessions.proposed.indexes.json"
  },
  "storage": { "rules": "storage.rules" },
  "functions": { "source": "functions" }
}
